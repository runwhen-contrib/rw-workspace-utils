images:
  - image: "$REPOSITORY_ROOT_PATH/runwhen-local"
    set_path: "runwhenLocal.image.tag"
  - image: "$REPOSITORY_ROOT_PATH/opentelemetry-collector"
    set_path: "opentelemetry-collector.image.tag"
  - image: "$REPOSITORY_ROOT_PATH/pushgateway"
    set_path: "runner.pushgateway.image.tag"
  - image: "$REPOSITORY_ROOT_PATH/runner"
    set_path: "runner.image.tag"
